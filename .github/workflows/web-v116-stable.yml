name: Legacy Web v116 Disabled

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Processo antigo v116 desativado; a versão Web oficial permanece v112."
