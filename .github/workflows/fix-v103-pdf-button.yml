name: Legacy PDF Fix Disabled

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow legado desativado
        run: echo "Correção de PDF agora é mantida diretamente no código Web."
