name: Legacy v103 Disabled

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow legado v103 desativado; versão Web é controlada pela rotina estável atual."
